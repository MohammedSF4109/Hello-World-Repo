# C# console application

trigger:
 - Master 

pool:
  name: Default
  
variables: 
 buildConfiguration: 'Release'

steps:  
  - task: PowerShell@2
    displayName: parsing XML document
    inputs:
      targetType: inline
      script: |
        $XMLfile = "$(Build.SourcesDirectory)\*.xml"
        $XMLNodes = [xml](Get-Content $XMLfile)
        $number = 0
        foreach($XMLNode in $XMLNodes.Build.Utilities)
        {
          $SourceType = $XMLData.Build.Utilities.Utility.SourceType
          if($SourceType -eq SVN) {Write-Host $number+1}
        }

  - task: 'DotNetCoreCLI@2'
    displayName: build $(buildConfiguration)
    inputs:
      command: build
      projects: '**/*.csproj'
      arguments: '--configuration $(buildConfiguration)'
  
  - task: 'DotNetCoreCLI@2'
    displayName: dotnet publish --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)
    inputs:
      command: publish
      publishWebProjects: false
      projects: '**/*.csproj'
      arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)'
      zipAfterPublish: true

  - task: 'PublishBuildArtifacts@1'
    displayName: publish artifact